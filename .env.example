# Supabase (public project settings — safe for frontend use with RLS enabled)
SUPABASE_URL=your-supabase-project-url
SUPABASE_ANON_KEY=your-supabase-anon-key

# JSearch API (RapidAPI) — powers Career Explorer, not wired up yet
JSEARCH_API_KEY=your-jsearch-rapidapi-key

# Anthropic Claude API — powers the FAQ chatbot, not wired up yet
ANTHROPIC_API_KEY=your-anthropic-api-key
